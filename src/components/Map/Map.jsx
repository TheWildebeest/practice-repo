import React, { Component } from "react";
import styles from "./Map.module.scss";

class Map extends Component {
  render() {
    return (
      <map id={styles.Map}>
      </map>
    );
  }
}

export default Map;
